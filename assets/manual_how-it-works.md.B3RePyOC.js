import{_ as t}from"./chunks/netstack-path.DlES-9oP.js";import{_ as i,o,c as a,a2 as r}from"./chunks/framework.yMGLHqpH.js";const m=JSON.parse('{"title":"How dae works","description":"","frontmatter":{"title":"How dae works"},"headers":[],"relativePath":"manual/how-it-works.md","filePath":"manual/how-it-works.md"}'),n={name:"manual/how-it-works.md"};function s(c,e,d,l,p,h){return o(),a("div",null,[...e[0]||(e[0]=[r('<div class="info custom-block"><p class="custom-block-title">INFO</p><p>Source: <a href="https://github.com/daeuniverse/dae/blob/5db27a0028d36e7847bd3796497df952337a20e2/docs/en/how-it-works.md" target="_blank" rel="noreferrer">dae upstream</a> · <a href="/repo-for-linux/upstream/dae-LICENSE.txt">AGPL-3.0 license</a>.</p></div><div lang="en-US"><h1 id="how-dae-works" tabindex="-1">How dae works <a class="header-anchor" href="#how-dae-works" aria-label="Permalink to &quot;How dae works&quot;">​</a></h1><p>dae operates by loading a program into the tc (traffic control) mount point in the Linux kernel using <a href="https://en.wikipedia.org/wiki/EBPF" target="_blank" rel="noreferrer">eBPF</a>. This program performs traffic splitting before the traffic enters the TCP/IP network stack. The position of tc in the Linux network protocol stack is illustrated in the diagram below (the diagram illustrates the receiving path, while the sending path is in the opposite direction), where netfilter represents the location of iptables/nftables.</p><p><img src="'+t+'" alt="Network Stack Path"></p><h2 id="traffic-splitting-principle" tabindex="-1">Traffic Splitting Principle <a class="header-anchor" href="#traffic-splitting-principle" aria-label="Permalink to &quot;Traffic Splitting Principle&quot;">​</a></h2><h3 id="splitting-criteria" tabindex="-1">Splitting Criteria <a class="header-anchor" href="#splitting-criteria" aria-label="Permalink to &quot;Splitting Criteria&quot;">​</a></h3><p>dae supports traffic splitting based on domain name, source IP, destination IP, source port, destination port, TCP/UDP, IPv4/IPv6, process name, MAC address, and other factors.</p><p>Among these, source IP, destination IP, source port, destination port, TCP/UDP, IPv4/IPv6, and MAC address can be obtained by parsing MACv2 frames.</p><p>The <strong>process name</strong> is obtained by monitoring local process socket, connect, and sendmsg system calls in the cgroupv2 mount point. It then reads and parses the command line from the process control block. This method is significantly faster than user-space programs like Clash that scan the entire procfs to obtain process information (the latter might take even tens of milliseconds).</p><p>The <strong>domain name</strong> is obtained by intercepting DNS requests and associating the requested domain name with the corresponding IP address. However, this method has some potential issues:</p><ol><li>It might lead to misjudgment. For example, if a domestic and a foreign website sharing the same IP address are accessed simultaneously within a short period, or if the browser employs DNS caching.</li><li>The user&#39;s DNS requests must traverse dae. This can be achieved by setting dae as the DNS server or using a public DNS while dae serves as the gateway.</li></ol><p>Despite these challenges, this approach is already an optimal solution compared to other methods. For instance, the Fake IP approach cannot perform IP-based splitting and is plagued by severe cache pollution issues. Similarly, domain sniffing can only intercept traffic like TLS/HTTP. While SNI sniffing for traffic splitting is effective, eBPF&#39;s limitations on program complexity and its lack of support for loops prevent us from implementing domain sniffing in the kernel space.</p><p>Hence, if DNS requests cannot pass through dae, domain-based splitting will not succeed.</p><blockquote><p>To mitigate DNS pollution and achieve improved CDN connection speeds, dae employs domain sniffing in user space. When <code>dial_mode</code> is set to &quot;domain&quot; or its variants and proxied traffic needs to be processed, dae sends the sniffed domain to the proxy server instead of sending the IP address. Consequently, the proxy server re-resolves the domain and connects using the optimal IP. This approach addresses DNS pollution and enhances CDN connection speed.</p><p>Additionally, advanced users who have used alternative splitting solutions and don&#39;t wish to route DNS requests through dae but still want certain traffic to be split based on domain (e.g., splitting traffic to Netflix nodes and download nodes based on the target domain, with some directly connecting via the core) can enforce the use of sniffed domains for splitting by setting <code>dial_mode: domain++</code>.</p></blockquote><p>dae achieves traffic splitting by redirecting traffic using the program in the tc mount point. The redirection is based on the splitting result, either redirecting the traffic to dae&#39;s tproxy port or allowing it to bypass dae and go directly.</p><h3 id="proxy-mechanism" tabindex="-1">Proxy Mechanism <a class="header-anchor" href="#proxy-mechanism" aria-label="Permalink to &quot;Proxy Mechanism&quot;">​</a></h3><p>The proxy mechanism of dae is akin to other programs. However, when binding to the LAN interface, dae leverages eBPF to directly associate the socket buffer of the traffic to be proxied in the tc mount point with the socket of dae&#39;s tproxy listening port. While binding to the WAN interface, dae transfers the socket buffer of the traffic to be proxied from the egress queue of the network card to the ingress queue. It also disables checksums and modifies the destination address to the tproxy listening port.</p><p>In terms of benchmarking, dae&#39;s proxy performance slightly surpasses that of other proxy programs, but the difference is not significant.</p><p>As of <a href="https://github.com/daeuniverse/dae/pull/458" target="_blank" rel="noreferrer">PR:implement stack bypass</a>, the hijack datapath has been changed to bypass stack for better performance and less stack influence (e.g. netfilter, systemd-sysctl). Please refer to the PR description for better understanding.</p><h3 id="direct-connection-mechanism" tabindex="-1">Direct Connection Mechanism <a class="header-anchor" href="#direct-connection-mechanism" aria-label="Permalink to &quot;Direct Connection Mechanism&quot;">​</a></h3><p>Conventionally, traffic splitting involves passing traffic through a proxy program, navigating the splitting module, and then determining whether to use a proxy or establish a direct connection. This process requires parsing, processing, and copying traffic through the network stack, delivering it to the proxy program, and subsequently copying, processing, and encapsulating it through the network stack before sending it out. This consumes substantial resources. Particularly in scenarios like BitTorrent downloads, even if a direct connection is set, it still consumes numerous connections, ports, memory, and CPU resources. It might even impact NAT type in gaming situations due to the proxy program&#39;s inadequate handling, resulting in connection errors.</p><p>dae performs traffic splitting at an earlier kernel stage, forwarding directly connected traffic through layer 3 routing. This approach reduces overhead by minimizing transitions between kernel and user space. At this point, Linux functions as a pure switch or router.</p><blockquote><p>For effective direct connection, advanced users with specific network topologies should ensure that, after configuring the <a href="/repo-for-linux/manual/user-guide/kernel-parameters.html">kernel parameters</a> and <strong>disabling</strong> dae, other devices can access the network normally when the device with dae is set as the gateway. For instance, accessing 223.5.5.5 should yield a &quot;UrlPathError&quot; response. When performing tcpdump on the dae-equipped device, request packets from client devices should be visible.</p></blockquote><p>Consequently, dae does not perform SNAT for directly connected traffic. In setups with a &quot;side-router,&quot; this leads to asymmetric routing. In this scenario, when sent out, traffic from client devices passes through dae to the gateway, but when received, traffic goes directly from the gateway to client devices, bypassing dae.</p><blockquote><p>Here, &quot;side-router&quot; refers to: 1) functioning as the gateway, 2) performing SNAT on TCP/UDP, and 3) having the LAN and WAN interfaces in the same network segment.</p><p>For example, if a laptop is at 192.168.0.3, the side-router is at 192.168.0.2, and the router is at 192.168.0.1, the logical three-layer topology would be: laptop -&gt; side-router -&gt; router. On the router side, only TCP/UDP traffic with a source IP of 192.168.0.2 would be visible, and no TCP/UDP traffic with a source IP of 192.168.0.3 would be present.</p><p>To our knowledge, we are the pioneers of this &quot;side-router&quot; definition (laughter).</p></blockquote><p>Asymmetric routing brings an advantage and a potential issue:</p><ol><li>It can enhance performance. Since return traffic doesn&#39;t traverse dae, direct connection performance becomes as swift as without a side-router, reducing the path.</li><li>It might disrupt stateful firewall&#39;s state maintenance and lead to packet loss (e.g., Sophos Firewall). However, this issue generally doesn&#39;t occur in home networks.</li></ol><p>From a benchmark perspective, dae&#39;s direct connectivity performance is formidable compared to other proxy solutions.</p></div>',2)])])}const g=i(n,[["render",s]]);export{m as __pageData,g as default};
